 $(function(){
 $('.ratinga2').each(function() {
                var ratingawidth = $(this).width(); 
                var stars = $(this).find('.ratinga').attr('data-value');
                $(this).find('.ratinga').width(Math.round(ratingawidth * (stars / 5)) + 'px');
            });
 });